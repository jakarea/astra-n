export default function Home() {
  // This will be handled by middleware - users will be redirected to /auth if not logged in
  // If logged in, they'll see the dashboard at /(dashboard)/page.tsx
  return null
}